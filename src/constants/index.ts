/**
 * 全局常量统一导出
 * 描述：所有常量都从这里导入，不再放在 src/constants/ 分类
 */

// 文档状态相关常量
export { statusConfig as docStatusConfig } from './doc-status';

// 状态选择器相关常量
export { statusConfig as statusSelectorConfig } from './status-selector';

